{
  "name": "justthetip-contracts",
  "version": "0.1.0",
  "description": "JustTheTip Solana Smart Contracts",
  "scripts": {
    "lint:fix": "cargo fmt --all && cargo clippy --fix --allow-dirty --allow-staged",
    "test": "anchor test",
    "build": "anchor build",
    "deploy:devnet": "anchor deploy --provider.cluster devnet",
    "deploy:mainnet": "anchor deploy --provider.cluster mainnet"
  },
  "dependencies": {
    "@coral-xyz/anchor": "^0.29.0"
  },
  "devDependencies": {
    "@solana/web3.js": "^1.87.0",
    "@types/chai": "^4.3.0",
    "@types/mocha": "^10.0.0",
    "chai": "^4.3.0",
    "mocha": "^10.0.0",
    "ts-mocha": "^10.0.0",
    "typescript": "^5.0.0"
  }
}
