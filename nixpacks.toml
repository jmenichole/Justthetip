# Nixpacks Configuration for Railway Deployment
# Discord Bot with API Server

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = []

[start]
# Start both the API server and Discord bot
cmd = "npm run start:bot-railway"
